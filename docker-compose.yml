services:
  mongo:
    image: mongo:7.0
    container_name: prismart-mongo
    restart: unless-stopped

    ports:
      - "27017:27017"

    environment:
      MONGO_INITDB_DATABASE: prismart

    volumes:
      - mongo_data:/data/db

  mongo-express:
    image: mongo-express:1.0.2
    container_name: prismart-mongo-express
    restart: unless-stopped

    depends_on:
      - mongo

    ports:
      - "8081:8081"

    environment:
      ME_CONFIG_MONGODB_SERVER: mongo
      ME_CONFIG_BASICAUTH: "false"

  api:
    image: prismart-api
    build:
      context: .
      dockerfile: Dockerfile

    container_name: prismart
    restart: unless-stopped

    depends_on:
      - mongo

    ports:
      - "${API_PORT:-3000}:3000"

    environment:
      API_PORT: ${API_PORT:-3000}
      MONGO_URI: mongodb://mongo:27017/prismart

    volumes:
      - .:/app
      - ./node_modules:/app/node_modules

volumes:
  mongo_data:
